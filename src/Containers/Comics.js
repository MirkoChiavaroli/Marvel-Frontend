import { Link } from "react-router-dom";

const Comics = () => {
  return (
    <dev>
      <dev>THIS IS Comics</dev>
      <Link to="/"> CLICCA QUI PER LA HOME </Link>
    </dev>
  );
};

export default Comics;

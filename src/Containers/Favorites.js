import { Link } from "react-router-dom";

const Favorites = () => {
  return (
    <dev>
      <dev>THIS IS Favorites</dev>
      <Link to="/"> CLICCA QUI PER LA HOME </Link>
    </dev>
  );
};

export default Favorites;

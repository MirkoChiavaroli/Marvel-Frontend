import { Link } from "react-router-dom";

const Home = () => {
  return (
    <dev>
      <dev>THIS IS HOME</dev>
      <Link to="/characters"> CLICCA QUI </Link>
    </dev>
  );
};

export default Home;
